import React from 'react';

import Base from './Base.js';

export default function Attribute (props) {
    return (
        <Base json={template}>
        </Base>
    );
}

const template ={
    id: 1,
    name: '???',
};
