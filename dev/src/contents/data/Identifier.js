import React from 'react';

import Base from './Base.js';

import {identifier} from '../../data/TEMPLATES.js';

export default function Identifier (props) {
    return (
        <Base json={identifier}>
        </Base>
    );
}
